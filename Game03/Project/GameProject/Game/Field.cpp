#include "Field.h"

Field::Field():Base(eType_Field)
{
	//m_Blue = COPY_RESOURCE("Blue", CImage);
	//m_ground_y = 540;
}

void Field::Draw()
{
	/*float sc;
	m_sky.Draw();
	
	sc = m_scroll.x / 4;
	m_town.SetRect(sc, 0, sc + 1280, 720);
	m_town.Draw();

	sc = m_scroll.x;
	sc = m_scroll.y;
	m_Blue.SetRect(sc, 0, sc + 1280, 720);
	m_Blue.Draw();*/
}